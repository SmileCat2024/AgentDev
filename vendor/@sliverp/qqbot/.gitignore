# 本包为 vendor 内化的 bundled 依赖（@sliverp/qqbot 未发布到 npm registry，
# 其依赖 silk-wasm/ws/mpg123-decoder 等随上游 tgz bundled 打包）。
# 必须整体入库（含 node_modules），否则干净环境无法解析。
# 父级 .gitignore 的 node_modules/ 规则在此反向放行（逐个精确放行 bundled 包）。
node_modules/*
!node_modules/@eshaz/
!node_modules/@wasm-audio-decoders/
!node_modules/mpg123-decoder/
!node_modules/silk-wasm/
!node_modules/simple-yenc/
!node_modules/ws/